@model CandyStoreApp.Models.Supplier

@{
    ViewData["Title"] = "Видалити постачальника";
}

<div class="container d-flex flex-column justify-content-center align-items-center mt-5">
    <div class="text-center mb-4">
        <h1 class="text-danger fw-bold">Видалити постачальника</h1>
        <p class="text-muted fs-5">
            Ви впевнені, що хочете видалити цього постачальника? <br />
            <span class="fst-italic">Цю дію не можна скасувати.</span>
        </p>
    </div>

    <div class="card shadow-lg p-4 col-md-6">
        <h4 class="text-muted text-center mb-4">Деталі постачальника</h4>
        <hr class="mb-4" />
        <dl class="row">
            <dt class="col-sm-6 fw-semibold">Назва постачальника:</dt>
            <dd class="col-sm-6">@Html.DisplayFor(model => model.SupplierName)</dd>

            <dt class="col-sm-6 fw-semibold">Контактна особа:</dt>
            <dd class="col-sm-6">@Html.DisplayFor(model => model.ContactPerson)</dd>

            <dt class="col-sm-6 fw-semibold">Номер телефону:</dt>
            <dd class="col-sm-6">@Html.DisplayFor(model => model.PhoneNumber)</dd>

            <dt class="col-sm-6 fw-semibold">Адреса постачальника:</dt>
            <dd class="col-sm-6">@Html.DisplayFor(model => model.SupplierAddress)</dd>
        </dl>

        <div class="text-center mt-4">
            <form asp-action="Delete" class="d-inline">
                @Html.AntiForgeryToken()
                <input type="hidden" asp-for="IdSupplier" />
                <button type="submit" class="btn btn-danger px-4 py-2 me-2">
                    <i class="bi bi-trash-fill"></i> Підтвердити видалення
                </button>
            </form>
            <a asp-action="Index" class="btn btn-secondary px-4 py-2">
                <i class="bi bi-arrow-left-circle"></i> Повернутися
            </a>
        </div>
    </div>
</div>
